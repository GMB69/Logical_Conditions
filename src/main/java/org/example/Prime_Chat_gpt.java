package org.example;

/*public class Prime_Chat_gpt {
    public static boolean isPrime(int n) {
        if (n <= 1) {
            return false;
        }
        for (int i = 2; i <= Math.sqrt(n); i++) {
            if (n % i == 0) {
                return false;
            }
        }
        return true;
   /*The isPrime method takes an integer as input and returns true if it is prime and false otherwise.
   The method first checks if the input number is less than or equal to 1, in which case it is not prime.
   Then it loops through all the integers from 2 to the square root of the input number, checking if any of
   them divide the input number evenly. If such an integer is found, then the input number is not prime,
   and the method returns false. If none are found, then the input number is prime, and the method returns true.

   You can call this method with any integer value to check if it is prime or not. For example:
    }
    public static void main(String[] args) {
        int n = 17;
        if (isPrime(n)) {
            System.out.println(n + " is prime");
        } else {
            System.out.println(n + " is not prime");
        }
    }

}
*/