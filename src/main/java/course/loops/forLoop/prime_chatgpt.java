package course.loops.forLoop;

/*public class prime_chatgpt {
    public class PrimeNumbers {
        public static void main(String[] args) {
            for (int i = 0; i <= 10000; i++) {
                if (isPrime(i)) {
                    System.out.print(i + " ");
                }
            }
        }
        public static boolean isPrime(int n) {
            if (n <= 1) {
                return false;
            }
            for (int i = 2; i <= Math.sqrt(n); i++) {
                if (n % i == 0) {
                    return false;
                }
            }
            return true;
        }
    }
}
/*The main method loops through all the integers from 0 to 10000 and calls the isPrime method for each number.
If isPrime returns true for a given number, then that number is printed out to the console.

You can run this code in a Java IDE or from the command line to see the output of all the prime numbers between 0 and 10000.*/